apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "meal-plan-app.fullname" . }}-test-connection"
  labels:
    {{- include "meal-plan-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "meal-plan-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
